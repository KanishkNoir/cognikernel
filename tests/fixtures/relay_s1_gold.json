{
  "_doc": "Frozen gold set for CogniKernel extraction regression. Derived from the deterministic reproduction of Relay session S1 (architecture chat). This is the HELD-OUT evaluation — extractor heads must NOT be trained on this session. Scoring is token+type based so it is version-agnostic across extractor implementations. See scripts/eval_extraction.py.",
  "session_label": "Relay S1 (architecture)",
  "transcript_session_id": "dcea0e3e-21f0-40b2-9be7-8aeb117eedc5",
  "baseline": {
    "_note": "Hand-scored from the failure analysis. The harness auto-score is a slightly looser yardstick (token+type cannot see every nuance the human judged); what matters is movement on the SAME yardstick before/after v1.",
    "events_produced": 60,
    "hand_precision": 0.38,
    "hand_clean_recall": 0.19,
    "hand_noise_rate": 0.42,
    "hand_hard_miss": 7
  },
  "targets_v1": {
    "precision_min": 0.85,
    "clean_recall_min": 0.70,
    "hard_miss_max": 3,
    "noise_rate_max": 0.10,
    "echo_events": 0,
    "meta_events": 0,
    "dup_events": 0,
    "truncated_events": 0,
    "event_count_min": 18,
    "event_count_max": 28
  },

  "type_compat": {
    "CONSTRAINT": ["CONSTRAINT_HARD", "CONSTRAINT_SOFT"],
    "DECISION": ["DECISION"],
    "GRAVEYARD": ["APPROACH_ABANDONED_DO_NOT_RETRY"],
    "THREAD": ["THREAD_OPEN", "THREAD_CLOSE"]
  },

  "ground_truth_facts": [
    {"id": "GT1",  "desc": "Stack (FastAPI/httpx/structlog/pydantic/Postgres/Redis)", "kind": "DECISION",
     "present": {"any": ["fastapi", "httpx", "structlog", "pydantic", "uvicorn"]},
     "clean":   {"any": ["fastapi", "uvicorn"]}},
    {"id": "GT2",  "desc": "OpenAI wire compatibility byte-for-byte", "kind": "CONSTRAINT",
     "present": {"any": ["byte-for-byte", "wire compat", "/v1/chat", "openai-compatible", "openai wire"]},
     "clean":   {"any": ["byte-for-byte", "wire compat"]}},
    {"id": "GT3",  "desc": "Streaming first-class, never buffered", "kind": "CONSTRAINT",
     "present": {"any": ["never buffered", "as they arrive", "first-class", "proxied as they arrive"]},
     "clean":   {"any": ["never buffered", "as they arrive", "proxied as they arrive"]}},
    {"id": "GT4",  "desc": "Secrets never in logs, scrubbed at boundary", "kind": "CONSTRAINT",
     "present": {"any": ["never touch logs", "scrubbed", "redact", "never logged", "never appear in logs", "never be passed to a logger", "[redacted]"]},
     "clean":   {"any": ["scrubbed at the logging boundary", "never be passed to a logger", "[redacted]"]}},
    {"id": "GT5",  "desc": "Upstream hard timeout 120s default / 300s hard max", "kind": "CONSTRAINT",
     "present": {"any": ["hard timeout", "total_timeout", "120 s", "300 s"]},
     "clean":   {"all": ["120", "300"]}},
    {"id": "GT6",  "desc": "Provider isolation — one provider can't crash gateway", "kind": "CONSTRAINT",
     "present": {"any": ["provider isolation", "one provider's failure", "cannot crash the gateway"]},
     "clean":   {"any": ["provider isolation"]}},
    {"id": "GT7",  "desc": "Routing: priority-ordered + per-deployment circuit breaker", "kind": "DECISION",
     "present": {"any": ["priority-ordered", "priority list", "circuit breaker", "round-robin"]},
     "clean":   {"any": ["priority-ordered", "priority list"]}},
    {"id": "GT8",  "desc": "Retry: 2 attempts/deployment, 500ms base x2 +-25% jitter 4s cap", "kind": "CONSTRAINT",
     "present": {"any": ["2 total attempts", "2 attempts", "500 ms", "retry policy", "backoff"]},
     "clean":   {"any": ["2 total attempts", "2 attempts", "500 ms"]}},
    {"id": "GT9",  "desc": "Fallback order frontier -> cheap -> local (degrading)", "kind": "DECISION",
     "present": {"any": ["frontier", "fallback order", "degrading"]},
     "clean":   {"all": ["frontier", "cheap", "local"]}},
    {"id": "GT10", "desc": "Default alias relay/default -> claude-sonnet-4-6", "kind": "DECISION",
     "present": {"any": ["relay/default", "default alias", "claude-sonnet"]},
     "clean":   {"any": ["claude-sonnet-4-6"]}},
    {"id": "GT11", "desc": "Rate limits in Redis, sliding-window, rl:{vkey}:rpm keys", "kind": "CONSTRAINT",
     "present": {"any": ["sliding window", "rpm", "tpm", "rate limit", "rl:"]},
     "clean":   {"all": ["redis"], "any": ["sliding window", "rl:"]}},
    {"id": "GT12", "desc": "Money as integer 10^-8 USD BIGINT, never float", "kind": "CONSTRAINT",
     "present": {"any": ["never float", "pure integer arithmetic", "minor unit", "integer units", "bigint"]},
     "clean":   {"any": ["10⁻⁸", "nano-dollar", "bigint"]}},
    {"id": "GT13", "desc": "Client auth: Bearer relay-sk-<token>", "kind": "DECISION",
     "present": {"any": ["relay-sk", "authenticate with a bearer", "client authenticate"]},
     "clean":   {"any": ["relay-sk"]}},
    {"id": "GT14", "desc": "Virtual keys stored as SHA-256 hash, never raw", "kind": "CONSTRAINT",
     "present": {"any": ["never store the raw key", "sha-256", "key_hash"]},
     "clean":   {"all": ["sha-256"], "any": ["key", "hash"]}},
    {"id": "GT15", "desc": "Provider API keys from env only, never in DB", "kind": "CONSTRAINT",
     "present": {"any": ["env var", "environment", "never in the database", "never touch the database", "relay_provider"]},
     "clean":   {"any": ["never in the database", "never touch the database", "env vars only"]}},
    {"id": "GT16", "desc": "Cache key = SHA-256 of canonical request; TTL 3600", "kind": "DECISION",
     "present": {"any": ["cache key", "resolved model", "canonically serialized", "3600", "1 hour"]},
     "clean":   {"any": ["canonically serialized request", "sha-256 of canonical"]}},
    {"id": "GT17", "desc": "Config: relay.yaml structure + env secrets, no interpolation", "kind": "DECISION",
     "present": {"any": ["relay.yaml", "yaml", "pydantic-settings", "no interpolation", "${var}"]},
     "clean":   {"all": ["relay.yaml"]}},
    {"id": "GT18", "desc": "Config validated at startup, fail-fast", "kind": "CONSTRAINT",
     "present": {"any": ["validated at startup", "fatal error", "fail-fast", "bad config"]},
     "clean":   {"any": ["validated at startup", "bad config is a fatal"]}},
    {"id": "GT19", "desc": "LangChain rejected in request path (graveyard)", "kind": "GRAVEYARD",
     "present": {"any": ["langchain"]},
     "clean":   {"all": ["langchain"]}},
    {"id": "GT20", "desc": "In-process rate limiting rejected (graveyard)", "kind": "GRAVEYARD",
     "present": {"any": ["in-process", "process-local", "1/n", "local cache has at most"]},
     "clean":   {"any": ["in-process"]}},
    {"id": "GT21", "desc": "Active thread: fallback+retry router end-to-end", "kind": "THREAD",
     "present": {"any": ["fallback+retry router", "active work thread", "router work thread"]},
     "clean":   {"any": ["fallback+retry router"]}}
  ],

  "noise_signatures": {
    "_doc": "An emitted event whose normalized text contains any of these is a false positive. echo = a verbatim user prompt; meta = the assistant narrating to the Stop hook.",
    "echo": [
      "propose the concrete stack and list every hard constraint",
      "decide the routing strategy and the per-deployment retry policy",
      "define the fallback chain",
      "where do the counters live",
      "design the usage ledger and be precise",
      "how do callers authenticate",
      "caching identical completions",
      "per-request timeout, and what must never appear in logs",
      "how is all of this configured and validated",
      "should we use langchain inside the request path"
    ],
    "meta": [
      "stop hook captures",
      "complete record of what was decided",
      "stated clearly so the"
    ]
  },

  "_baseline_event_hand_labels": {
    "_doc": "The 60 events from the deterministic S1 reproduction, hand-labeled VALID/DEGRADED/NOISE for transparency. Not used by the scorer (the scorer is token+type based); kept so the analysis is auditable.",
    "VALID": [6, 8, 17, 18, 20, 22, 26, 28, 30, 33, 40, 41, 42, 44, 45, 46, 47, 49, 52, 53, 55, 56, 58],
    "DEGRADED": [2, 3, 4, 5, 12, 23, 29, 34, 38, 50, 54, 57],
    "NOISE": [1, 7, 9, 10, 11, 13, 14, 15, 16, 19, 21, 24, 25, 27, 31, 32, 35, 36, 37, 39, 43, 48, 51, 59, 60],
    "clean_recall_facts": ["GT4", "GT6", "GT15", "GT18"],
    "hard_miss_facts": ["GT7", "GT8", "GT9", "GT11", "GT13", "GT19", "GT20"]
  }
}
